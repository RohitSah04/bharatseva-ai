# Frontend environment — Vite dev server
# Leave VITE_API_BASE_URL empty so all /api requests go through the Vite proxy.
# The proxy target is set in vite.config.js (defaults to http://localhost:5000).
VITE_API_BASE_URL=
